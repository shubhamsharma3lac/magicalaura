import { ElectronicConfigurationPipe } from './electronic-configuration.pipe';

describe('ElectronicConfigurationPipe', () => {
  it('create an instance', () => {
    const pipe = new ElectronicConfigurationPipe();
    expect(pipe).toBeTruthy();
  });
});
